#include "Shifting.h"

// This translation unit is currently a thin wrapper that only provides the
// Shifting / HarmonicPitchDetector implementations declared in Shifting.h.
// All logic is in the header so the block can be inlined easily into the
// audio path without additional linkage complexity.

